---
---

<!-- Fixed Luxury Top Navigation Bar (Visible by default on first load) -->
<header id="main-navbar" class="fixed top-0 left-0 w-full z-40 bg-black/85 backdrop-blur-md border-b border-white/10 transition-all duration-500 py-3.5 px-4 sm:px-6 md:px-8 select-none">
  <div class="w-full max-w-6xl mx-auto flex items-center justify-between">
    
    <!-- Left: Brand Logo -->
    <a href="/" class="flex items-center gap-2 group shrink-0">
      <img 
        src="/assets/new-logo.png" 
        alt="Shirui Wellness Spa Logo" 
        class="h-9 sm:h-11 w-auto object-contain transition-transform duration-500 group-hover:scale-105"
      />
    </a>

    <!-- Center: Desktop Navigation Links (Visible on Desktop) -->
    <nav class="hidden lg:flex items-center gap-9 text-[13px] tracking-[0.16em] uppercase font-semibold">
      <a href="#about" class="text-white/80 hover:text-gold transition-colors duration-300">About</a>
      <a href="#services" class="text-white/80 hover:text-gold transition-colors duration-300">Treatments</a>
      <a href="#packages" class="text-white/80 hover:text-gold transition-colors duration-300">Packages</a>
      <a href="#testimonials" class="text-white/80 hover:text-gold transition-colors duration-300">Reviews</a>
      <a href="#contact" class="text-white/80 hover:text-gold transition-colors duration-300">Contact</a>
    </nav>

    <!-- Right: Desktop CTA Buttons (Visible on Desktop) -->
    <div class="hidden sm:flex items-center gap-3 shrink-0">
      <button 
        class="open-booking-btn px-6 py-2.5 rounded-full font-bold tracking-[0.16em] text-xs text-black bg-gradient-to-r from-gold via-gold-light to-gold-dark hover:scale-105 uppercase transition-all duration-300 shadow-[0_0_18px_rgba(212,175,55,0.35)] whitespace-nowrap"
      >
        Book Session
      </button>
    </div>

    <!-- Mobile Right: Hamburger Menu Trigger -->
    <div class="flex lg:hidden items-center gap-2 shrink-0">
      <button 
        id="mobile-menu-btn" 
        class="text-gold p-2 rounded-xl border border-gold/40 hover:border-gold bg-black/70 hover:bg-gold/15 transition-colors focus:outline-none flex items-center justify-center"
        aria-label="Open Navigation Menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

  </div>
</header>

<!-- Mobile Navigation Drawer Overlay -->
<div 
  id="mobile-nav-drawer" 
  class="fixed inset-0 bg-black/95 backdrop-blur-2xl z-50 pointer-events-none opacity-0 transition-all duration-500 flex flex-col justify-between p-6 select-none"
>
  <!-- Top Header in Drawer -->
  <div class="flex items-center justify-between border-b border-white/10 pb-4">
    <img 
      src="/assets/new-logo.png" 
      alt="Shirui Wellness Spa" 
      class="h-10 w-auto object-contain"
    />
    <button 
      id="mobile-drawer-close" 
      class="text-white/70 hover:text-gold p-2 focus:outline-none"
      aria-label="Close Navigation Menu"
    >
      <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>
  </div>

  <!-- Center Navigation Links -->
  <nav class="flex flex-col items-center gap-6 my-auto text-base tracking-[0.18em] uppercase font-semibold text-white">
    <a href="#about" class="drawer-link hover:text-gold transition-colors duration-300">About Us</a>
    <a href="#services" class="drawer-link hover:text-gold transition-colors duration-300">Treatments</a>
    <a href="#packages" class="drawer-link hover:text-gold transition-colors duration-300">Packages</a>
    <a href="#pressure" class="drawer-link hover:text-gold transition-colors duration-300">Therapy Selector</a>
    <a href="#philosophy" class="drawer-link hover:text-gold transition-colors duration-300">Philosophy</a>
    <a href="#testimonials" class="drawer-link hover:text-gold transition-colors duration-300">Reviews</a>
    <a href="#hygiene" class="drawer-link hover:text-gold transition-colors duration-300">Hygiene</a>
    <a href="#contact" class="drawer-link hover:text-gold transition-colors duration-300">Contact Us</a>
  </nav>

  <!-- Bottom Actions in Drawer -->
  <div class="flex flex-col gap-3.5 pt-4 border-t border-white/10">
    <div class="grid grid-cols-2 gap-3">
      <a 
        href="tel:+919030069708" 
        class="py-3.5 rounded-full border border-gold/50 text-center font-bold tracking-wider text-xs text-gold uppercase bg-black/40"
      >
        Call Now
      </a>
      <a 
        href="https://wa.me/919030069708" 
        target="_blank"
        rel="noopener noreferrer"
        class="py-3.5 rounded-full border border-emerald-500/50 text-center font-bold tracking-wider text-xs text-emerald-400 uppercase flex items-center justify-center gap-1.5 bg-emerald-950/20"
      >
        <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
        WhatsApp
      </a>
    </div>
    <button 
      class="open-booking-btn w-full py-4 rounded-full font-bold tracking-[0.16em] text-xs text-black bg-gradient-to-r from-gold via-gold-light to-gold-dark hover:scale-[1.02] uppercase transition-all duration-300 shadow-[0_0_20px_rgba(212,175,55,0.35)]"
    >
      Book Appointment
    </button>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const navbar = document.getElementById('main-navbar');
    const menuBtn = document.getElementById('mobile-menu-btn');
    const closeBtn = document.getElementById('mobile-drawer-close');
    const drawer = document.getElementById('mobile-nav-drawer');
    const drawerLinks = document.querySelectorAll('.drawer-link');

    // Scroll state change for navbar
    window.addEventListener('scroll', () => {
      if (window.scrollY > 40) {
        navbar?.classList.add('bg-black/95', 'shadow-xl', 'border-gold/20', 'py-3');
        navbar?.classList.remove('bg-black/85', 'py-3.5');
      } else {
        navbar?.classList.remove('bg-black/95', 'shadow-xl', 'border-gold/20', 'py-3');
        navbar?.classList.add('bg-black/85', 'py-3.5');
      }
    });

    function openDrawer() {
      drawer?.classList.remove('pointer-events-none');
      drawer?.classList.replace('opacity-0', 'opacity-100');
      document.body.classList.add('overflow-hidden');
    }

    function closeDrawer() {
      drawer?.classList.add('pointer-events-none');
      drawer?.classList.replace('opacity-100', 'opacity-0');
      document.body.classList.remove('overflow-hidden');
    }

    menuBtn?.addEventListener('click', openDrawer);
    closeBtn?.addEventListener('click', closeDrawer);

    drawerLinks.forEach(link => {
      link.addEventListener('click', closeDrawer);
    });
  });
</script>
